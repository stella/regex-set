# Use sparse index for faster resolution.
[registries.crates-io]
protocol = "sparse"

[alias]
ci-fmt = "fmt --all -- --check"
ci-clippy = "clippy --all-targets --all-features --locked -- -D warnings"
ci-test = "test --all-features --locked"
