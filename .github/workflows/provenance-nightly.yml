name: Provenance Nightly

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  provenance-update:
    permissions:
      contents: write
      pull-requests: write
    uses: stella/.github/.github/workflows/provenance-update.yml@f8027309fa1541b5c7a394a0cdd3fd0baeb9a523
    secrets: inherit
    with:
      install-command: bun install --frozen-lockfile
      provenance-version: v0.1.1
