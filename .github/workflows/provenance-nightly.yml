name: Provenance Nightly

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  provenance-update:
    permissions:
      contents: write
      pull-requests: write
    uses: stella/.github/.github/workflows/provenance-update.yml@7198083e27bdd963cacc0b7d70ca54fe3ec83393
    secrets: inherit
    with:
      install-command: bun install --frozen-lockfile
      provenance-version: v0.1.3
