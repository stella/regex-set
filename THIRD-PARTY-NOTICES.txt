Third-Party Notices

This project directly depends on the following
runtime and build-time components:

Rust runtime dependencies
- fancy-regex 0.17.0 — MIT
- napi 3.8.3 — MIT
- napi-derive 3.5.2 — MIT
- regex 1.12.3 — MIT OR Apache-2.0
- regex-automata 0.4.14 — MIT OR Apache-2.0
- regex-syntax 0.8.10 — MIT OR Apache-2.0
- unicode-segmentation 1.12.0 — MIT OR Apache-2.0

Rust build dependency
- napi-build 2.3.1 — MIT

JavaScript/WASM runtime dependency
- @napi-rs/wasm-runtime 1.1.1 — MIT

The repository also contains development-only
tooling dependencies used for building, testing,
linting, and release automation. Machine-generated
SBOMs are produced in CI as artifacts instead of
being committed to the repository, because the
platform-specific optional packages differ by build
environment.
